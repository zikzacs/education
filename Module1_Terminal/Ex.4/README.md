500v2 явялется второй версией это скрипта.

При котором текстовые файлы, исполняемые файлы и пустые директории выводятся поочередно а не все подряд,
также при их отсуствии выводит информацию на экран
